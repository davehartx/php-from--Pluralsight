<?php

    $languages = [
        'PHP' => 'PHP: Hypertext Preprocessor',
        'VB' => 'Visual Basic'
    ];

    foreach ($languages as $index => $language) {
        echo "$index: $language<br>";
    }